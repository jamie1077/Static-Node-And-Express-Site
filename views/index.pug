extends layout.pug

block content

  article.portfolio-intro
    h1 My Portfolio

    p Here are some of the projects I have worked on as a student at Treehouse that showcase my skills in JavaScript


  article.portfolio-project-thumbs
    .projects-box.box.grid
      each project in projects 
        .cell
          span(class=`project${project.id}`)  
            img.thumbnail(src=`${project.image_urls[0]}` alt=`Thumbnail of ${project.project_name}`)
          a(href=`/projects/${project.id}`)
            
            h5= project.project_name
      